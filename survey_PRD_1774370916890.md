# Product Requirements Document: Stress and Mental Health in College Students Survey

## Overview

Build a short web-based survey targeted at undergraduate college students that collects information about their mental health and stress levels. The app has two pages: a survey form and a results page showing aggregated responses. Responses are stored in a Supabase PostgreSQL database.

---

## Tech Stack

- **Framework:** Vite + React
- **Database:** Supabase (PostgreSQL)
- **Hosting:** Azure Static Web Apps
- **Charting:** Recharts

---

## Survey Questions

The survey must include the following seven questions, using the specified input types:

| #   | Question                                                          | Input Type    |
| --- | ----------------------------------------------------------------- | ------------- |
| 1   | What is your current year in college?                             | Dropdown      |
| 2   | What is your major or field of study?                             | Text input    |
| 3   | How often do you feel stressed during a typical week?             | Radio buttons |
| 4   | What are your biggest sources of stress?                          | Checkboxes    |
| 5   | How do you usually cope with stress?                              | Checkboxes    |
| 6   | How would you rate your overall mental health?                    | Radio buttons |
| 7   | Do you feel your college provides enough mental health resources? | Radio buttons |

### Question Details

**Q1 — Year in College**

- Dropdown list with these options:
  - freshman
  - sophomore
  - junior
  - senior
- Required field

**Q2 — Major**

- Text input
- Required field

**Q3 — Stress Frequency**

- Radio button group with the following options (select one):
  - never
  - rarely
  - sometimes
  - often
  - always
- Required field

**Q4 — Source of Stress (select all that apply)**

- Checkboxes with the following options:
  - academic workload
  - exams/tests
  - financial concerns
  - social life
  - family expectations
  - health problems
  - time management
  - post-grad employment
  - Other
- If "Other" is checked, display a text input box asking the user to describe their other hobby
- At least one checkbox must be selected (required)
- The "Other" text box is required if "Other" is checked

**Q5 — Coping Methods (Checkboxes)**

- Select all that apply:
  - Exercise
  - Talking to friends/family
  - Watching TV/streaming
  - Sleeping
  - Studying more
  - Social media
  - Counseling
  - Other
- If "Other" is checked → show text input
- At least one required

**Q6 — Mental Health Rating (Radio Buttons)**

- Options:
  - 1 (Very poor)
  - 2 (Poor)
  - 3 (Average)
  - 4 (Good)
  - 5 (Excellent)
- Required field

**Q7 — School Support (Radio Buttons)**

- Options:
  - Yes
  - No
  - Not sure
- Required field

---

## Form Behavior

- All fields are required unless conditionally hidden
- Display inline validation errors using aria-describedby "Other" inputs appear dynamically when selected
- On submission:
  - Show confirmation screen with user responses
  - Buttons:
    - Submit Another Response
    - View Results
- Show loading state: "Submitting..."
- Handle database errors gracefully

---

## Results Page

A separate `/results` route displays aggregated, anonymized data from all submissions. Individual responses are never shown — only group-level summaries.

### Charts / Visualizations

- Total Responses — large number at top
- Stress Frequency Distribution
  - Bar chart showing counts for Never → Always
- Mental Health Ratings
  - Bar chart (1–5 scale)
- Top Stress Sources
  - Horizontal bar chart (aggregated checkbox counts)
  - Normalize "Other" responses
- Coping Methods
  - Horizontal bar chart
  - Include normalized "Other" entries
- Perception of School Support
  - Pie chart or bar chart (Yes / No / Not sure)

Use a charting library (e.g. Recharts) for all visualizations.

### Navigation

- Home page has a short welcome message with a "Take the Survey" button linking to the survey form and a "View Results" button linking to the results page
- The results page has a "Home" button linking back to the home page
- The survey form has a "View Results" button in the header linking to the results page

---

## Database

I am using my own Supabase account. Run the following SQL in the Supabase SQL Editor to create the table and configure access.

### Create Table

```sql
create table mental_health_survey_results (
  id uuid primary key default gen_random_uuid(),
  created_at timestamptz default now(),
  year_in_college text not null,
  major text not null,
  stress_frequency text not null,
  stress_sources text[] not null,
  stress_sources_other text,
  coping_methods text[] not null,
  coping_other text,
  mental_health_rating int not null,
  school_support text not null
);
```

### Row-Level Security

Supabase enables RLS by default and will block all inserts and selects unless policies are configured. Because this is a public survey with no authentication, run the following:

```sql
alter table mental_health_survey_results enable row level security;

create policy "Allow public insert"
  on mental_health_survey_results for insert
  to anon
  with check (true);

create policy "Allow public select"
  on mental_health_survey_results for select
  to anon
  using (true);
```

### Environment Variables

The app connects to Supabase using two environment variables:

| Variable                 | Description                                                 |
| ------------------------ | ----------------------------------------------------------- |
| `VITE_SUPABASE_URL`      | Your Supabase project URL (e.g. `https://xxxx.supabase.co`) |
| `VITE_SUPABASE_ANON_KEY` | Your Supabase anon/public key                               |

Both values are found in the Supabase dashboard under **Project Settings → API**.

- For local development, create a `.env` file in the project root with these values. Add `.env` to `.gitignore`.
- For production, add both as GitHub Actions secrets. The CI/CD workflow injects them at build time.

---

## Design & Quality Standards

### Appearance

- Simple, minimalist design throughout
- White or light neutral background with dark body text
- `#8A3BDB` is the primary accent color — use it for buttons, links, active/focus states, borders, and chart colors
- Do not use `#8A3BDB` for body text; it does not meet WCAG AA contrast requirements at small sizes. Use a dark neutral (e.g. `#1a1a1a`) for body text
- For any text rendered in the accent color (e.g. headings or labels), verify contrast passes WCAG AA before using it

### Responsive Layout

The app must be usable on mobile, tablet, and desktop screen sizes. Use a single-column layout on small screens. Do not assume desktop-only use — the survey will likely be completed on phones.

### HTML5 Validation

All pages must validate as HTML5 with no errors using the [W3C Markup Validation Service](https://validator.w3.org/).

### Accessibility — WCAG 2.1 Level AA

All pages must pass WCAG 2.1 Level AA. Key requirements:

- All form inputs must have a programmatically associated `<label>` element
- Inline validation error messages must be programmatically associated with their fields using `aria-describedby`
- The "Other" hobby text input must be keyboard accessible when it appears dynamically — focus should move to it automatically when "Other" is checked
- All images and icons must have appropriate `alt` text
- Color alone must not be used to convey meaning (e.g. error states must include text, not just a red border)
- All interactive elements must be reachable and operable via keyboard
- Use sufficient color contrast for all text — minimum 4.5:1 for normal text, 3:1 for large text

---

## Footer

Every page displays a footer with the following text:

> Survey by Brianne Bonus, BAIS:3300 - spring 2026.

---

## Deployment

This app is deployed as an **Azure Static Web App** using Vite + React.

Because the app uses client-side routing (`/` and `/results`), a `staticwebapp.config.json` file must be included in the `public/` directory so Azure serves `index.html` for all routes instead of returning 404s on direct navigation or page refresh.

```json
{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/assets/*"]
  }
}
```

This file is automatically copied to `dist/` during the Vite build.
